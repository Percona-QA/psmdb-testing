name: PBM-FULL

on:
  workflow_dispatch:
    inputs:
      pbm_branch:
        description: "pbm branch"
        required: false
      go_ver:
        description: "golang version"
        required: false

#  push:
#    branches:
#      - main

#  pull_request:
#    branches:
#      - main

jobs:
  test:
    runs-on: ubuntu-20.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        psmdb: ["4.2", "4.4", "5.0", "6.0"]
        test: [logical, physical, incremental]
    env:
      PBM_BRANCH: ${{ github.event.inputs.pbm_branch || 'main' }}
      GO_VER: ${{ github.event.inputs.go_ver || 'latest' }}
    steps:
    - uses: actions/checkout@v2
    - name: Test ${{ matrix.test }} backup/restore on PSMDB ${{ matrix.psmdb }} PBM branch ${{ env.PBM_BRANCH }}
      run: |
        PSMDB=percona/percona-server-mongodb:${{ matrix.psmdb }} docker-compose build
        docker-compose up -d
        docker-compose run test pytest -s --junitxml=junit.xml -k ${{ matrix.test }}
      working-directory: ./pbm-functional/pytest
    - name: Publish Test Report
      uses: mikepenz/action-junit-report@v3
      if: success() || failure() 
      with:
        report_paths: '**/junit.xml'

