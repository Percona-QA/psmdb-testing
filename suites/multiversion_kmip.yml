test_kind: js_test

selector:
  roots:
  - jstests/multiVersion/genericSetFCVUsage/do_upgrade_downgrade.js

executor:
  config:
    shell_options:
      nodb: ''
      global_vars:
        TestData:
          enableEncryption: ''
          kmipServerName: vault
          kmipPort: 5696
          kmipServerCAFile: /etc/kmip/ca-bundle.pem
          kmipClientCertificateFile: /etc/kmip/mongod-kmip-client.pem
          encryptionCipherMode: AES256-CBC
