test_kind: js_test

selector:
  roots:
  - jstests/multiVersion/genericSetFCVUsage/do_upgrade_downgrade.js

executor:
  config:
    shell_options:
      nodb: ''
      global_vars:
        TestData:
          enableEncryption: ''
          vaultServerName: vault
          vaultPort: 8200
          vaultTokenFile: /etc/vault/vault.token
          vaultSecret: secret_v2/data/psmdb/test
          vaultServerCAFile: /etc/vault/ca.crt
          encryptionCipherMode: AES256-CBC
