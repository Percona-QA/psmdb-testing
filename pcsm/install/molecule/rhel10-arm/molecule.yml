---
dependency:
  name: galaxy
driver:
  name: ec2
platforms:
  - name: rhel10-arm-${BUILD_NUMBER}-${JOB_NAME}
    region: us-west-2
    image: "${MOLECULE_RHEL10_ARM_AMI:-ami-0697a1da79ebefa36}"
    vpc_subnet_id: subnet-07f71e1a3117a32f9
    instance_type: c6g.large
    ssh_user: ec2-user
    root_device_name: /dev/sda1
    instance_tags:
      iit-billing-tag: jenkins-psmdb-worker
      job-name: ${JOB_NAME}
provisioner:
  name: ansible
  log: True
  playbooks:
    create: ../../../../playbooks/create.yml
    destroy: ../../../../playbooks/destroy.yml
    prepare: ../../playbooks/prepare.yml
    converge: ../../playbooks/redhat_podman.yml
  env:
    ANSIBLE_SSH_TRANSFER_METHOD: piped
verifier:
  name: testinfra
  directory: ../../../tests/
  options:
    verbose: true
    s: true
    junitxml: rhel10-arm-report.xml
scenario:
  name: rhel10-arm
  destroy_sequence:
    - destroy
  test_sequence:
    - destroy
    - create
    - prepare
    - converge
    - verify
    - destroy
