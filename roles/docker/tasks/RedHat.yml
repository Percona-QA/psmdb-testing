- set_fact:
    # User to log into each AWS Instance
    ssh_user: "{{ lookup('env', 'SSH_USER') | default('none', true) }}"

- name: Remove podman-docker if present
  yum:
    name: podman-docker
    state: absent
  when: ansible_os_family == "RedHat"

- name: Add Docker CE repository
  get_url:
    url: https://download.docker.com/linux/centos/docker-ce.repo
    dest: /etc/yum.repos.d/docker-ce.repo
    mode: '0644'
  register: download
  until: download is succeeded
  retries: 5
  delay: 3
  when:
    - ansible_os_family == "RedHat"

- name: Force release server to use 9 in Docker repo for AL2023/RHEL10 compatibility
  replace:
    path: /etc/yum.repos.d/docker-ce.repo
    regexp: '\$releasever'
    replace: '9'
  when: >
    ansible_distribution == "Amazon" or
    (ansible_distribution == "RedHat" and ansible_distribution_major_version == "10")

- name: Install dependencies
  yum:
    name: "{{ packages }}"
    state: present
    update_cache: yes
  vars:
    packages:
      - wget
      - docker-ce
      - docker-ce-cli
      - containerd.io
  when: ansible_os_family == "RedHat"

- name: Install iptables-legacy for Docker on RHEL 10
  yum:
    name: iptables-legacy
    state: present
  when:
    - ansible_distribution == "RedHat"
    - ansible_distribution_major_version == "10"

- name: Switch iptables alternative to legacy for Docker on RHEL 10
  community.general.alternatives:
    name: iptables
    path: /usr/sbin/iptables-legacy
  when:
    - ansible_distribution == "RedHat"
    - ansible_distribution_major_version == "10"

- name: Enable and start Docker service
  service:
    name: docker
    state: started
    enabled: true

- name: Ensure docker group exists
  group:
    name: docker
    state: present

- name: Add user ssh-user to docker group
  user:
    name: "{{ ssh_user }}"
    groups: docker
    append: yes
