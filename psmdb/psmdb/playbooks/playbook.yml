---
- name: Converge
  hosts: all
  become: true
  become_method: "{{ ansible_become_method | default('sudo') }}"
  roles:
    - role: '../../../roles/easyrsa'
    - role: psmdb
    - role: '../../../roles/docker'
    - role: '../../../roles/openldap'
    - role: '../../../roles/kmip-vault'
    - role: '../../../roles/kerberos-docker'
    - role: '../../../roles/keycloak'
